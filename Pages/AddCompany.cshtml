@page "/AddCompany"
@using System.ComponentModel.DataAnnotations
@using System.Text.RegularExpressions
@using Microsoft.AspNetCore.Components
@using Microsoft.EntityFrameworkCore
@using MudBlazor
@using SkinAPI.GUI
@model SkinAPI.GUI.Pages.AddCompany

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>Add Company - SkinConsult</title>
</head>
<body>
<div>
    @code {
        InputModel _inputModel = new InputModel();
        MudForm form;
    }
    <MudCard>
        <MudForm Model="@_inputModel" @ref="@form">
            <MudCardContent>
                <MudTextField @bind-value="_inputModel.CompanyName"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.Streetname"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.Postalcode"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.City"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.Country"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.GeneralEmail"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.GeneralPhonenr"
                              immediate="true"
                              label="Company name"/>
                <MudTextField @bind-value="_inputModel.Website"
                              immediate="true"
                              label="Company name"/>
            </MudCardContent>
        </MudForm>
    </MudCard>

    @code {

        public class InputModel
        {
            public string CompanyName { get; set; }
            public string Streetname { get; set; }
            public string Postalcode { get; set; }
            public string City { get; set; }
            public string Country { get; set; }
            public string GeneralEmail { get; set; }
            public string GeneralPhonenr { get; set; }
            public string Website { get; set; }

        }

    }

</div>
</body>
</html>